/**
 * The user agent header to use when making a request to an API that requires one. Note that an accept header is also required for some APIs.
 * @see {@link https://scryfall.com/docs/api}
 * @see {@link https://docs.discord.com/developers/reference#user-agent}
 * @internal
 */
const userAgent = "Maestro/0.1.0";

export default userAgent;
