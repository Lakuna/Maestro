/**
 * Discord sticker format type.
 * @see {@link https://docs.discord.com/developers/resources/sticker#sticker-object-sticker-format-types}
 * @internal
 */
enum StickerFormatType {
	PNG = 1,
	APNG = 2,
	LOTTIE = 3,
	GIF = 4
}

export default StickerFormatType;
