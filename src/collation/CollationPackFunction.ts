/**
 * A function that takes an optional seed and returns a list of collector numbers of cards in a given set.
 * @internal
 */
export type CollationPackFunction = (seed?: number) => readonly string[];
