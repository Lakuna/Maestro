/**
 * A function that takes an optional seed and returns a list of collector's numbers of cards in a given set.
 * @internal
 */
export type CollationPackFunction = (seed?: number) => readonly number[];
