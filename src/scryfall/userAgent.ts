/**
 * The user agent header to use when making a request to the Scryfall API. Note that an accept header is also required.
 * @see {@link https://scryfall.com/docs/api | API Documentation}
 * @internal
 */
const userAgent = "Maestro/0.1.0";

export default userAgent;
